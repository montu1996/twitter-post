const React = require( 'react' );

const {Component} = wp.element;

class Save extends Component {
    constructor() {
        super(...arguments);
    }

    render() {
        return (
            <h1>Hello World</h1>
        );
    }
}

export default Save;
